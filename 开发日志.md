# obsidian-trilium-sync 开发日志

## 2026-05-28（Bug 修复 — Loose List 支持）

### 问题

v0.1.3 把列表项合并到一个 `<ul>`，但丢弃了空行间距。用户日记中列表项之间的空行在 Trilium 中消失。

### 根因

Markdown 标准中，列表项之间有空行叫 **loose list**，每个 `<li>` 内容应包裹 `<p>` 标签，`<p>` 的 margin 提供自然间距。v0.1.3 只做了合并，没处理间距。

### 修复

- step 10 检测列表项前是否有空行，标记为 `looseItems`
- `flushList` 构建 `<ul>` 时，如果是 loose list，每个 `<li>` 内容包裹 `<p>` 标签
- 紧凑列表（无空行）不受影响

### 模拟测试 9/9 通过

| 场景 | 结果 |
|------|------|
| loose list（1行空行间隔） | ✅ 1个 ul，有 `<p>` |
| tight list（无空行） | ✅ 1个 ul，无 `<p>` |
| 段落之间 | ✅ 保留空行 |
| 列表后跟段落 | ✅ 有间隔 |
| 列表后跟标题 | ✅ 有间隔 |
| 2行空行的 loose list | ✅ 有 `<p>` |
| checkbox loose list | ✅ 有 `<p>` |
| 列表+段落+列表 | ✅ 两个独立列表 |
| 用户实际日记 | ✅ 有 `<p>` |

### Release

v0.1.4: https://github.com/laozhangge/obsidian-trilium-sync/releases/tag/v0.1.4

---

## 2026-05-28（Bug 修复 — 列表间空行终极修复）

### 问题

v0.1.1 和 v0.1.2 的修复都是在 step 15 做补丁，效果不理想：
- v0.1.1：完全删除空行 → 列表间无间隔
- v0.1.2：降级为单个 `<br>` → 仍有 2 行（`<ul>` 自带 margin 叠加）

### 根因

**问题在 step 10，不在 step 15！**

step 10 的列表处理中，空行触发了 `flushList()`，把本应合并的一个 `<ul>` 拆成了多个独立的 `<ul>`。每个 `<ul>` 自带 `margin-top/bottom`，加上 `<br><br>` 就变成了 3-4 行视觉空隙。

```
输入:  * 早晨：内容\n\n* 上午：内容
旧版:  <ul><li>早晨</li></ul><br><br><ul><li>上午</li></ul>  ← 两个ul，margin叠加
新版:  <ul><li>早晨</li><li>上午</li></ul>                    ← 一个ul，CSS控制间距
```

### 修复

step 10 中添加 `hasListAhead` 辅助函数：空行在列表占位符之间时，跳过空行不 flush，保持列表项在同一个 `<ul>` 内。支持跳过连续多个空行。

回退了 step 15 的 `structuralTags` 降级逻辑（不再需要）。

### 模拟测试 9/9 通过

| 场景 | 结果 |
|------|------|
| 用户日记（1行空行间隔） | ✅ 合并为 1 个 ul |
| 段落之间（1行空行） | ✅ 保留空行 |
| 列表后跟段落 | ✅ 有间隔 |
| 列表后跟标题 | ✅ 有间隔 |
| 2行空行的列表 | ✅ 合并为 1 个 ul |
| 3行空行的列表 | ✅ 合并为 1 个 ul |
| 无空行的连续列表 | ✅ 1 个 ul |
| 列表+段落+列表 | ✅ 两个独立列表 |
| checkbox 列表 | ✅ 合并为 1 个 ul |

### Release

v0.1.3: https://github.com/laozhangge/obsidian-trilium-sync/releases/tag/v0.1.3

---

## 2026-05-28（Bug 修复 — 列表间空行过多 v2）

### 问题

v0.1.1 修复过于激进：完全删除了结构性块元素之间的 `___BRBR___`，导致列表间完全没有空行。

### 修复

将策略从"删除"改为"降级"：
- `</ul><br><br><ul>` → `</ul><br><ul>`（1行空行 + 列表自带 margin = 适中间距）
- `</p><br><br><p>` → 不变（段落间空行完整保留）
- 多行空行也降级为 1 行（`(___BRBR___)+` 正则）

### 修复效果对比

| 场景 | 原版 | v0.1.1 | v0.1.2 |
|------|------|--------|--------|
| 1行空行-列表之间 | 4行 | 0行 ❌ | 1行 ✅ |
| 1行空行-段落之间 | 2行 | 2行 | 2行 ✅ |
| 2行空行-列表之间 | 6行 | 0行 ❌ | 1行 ✅ |
| 2行空行-段落之间 | 4行 | 4行 | 4行 ✅ |

### Release

v0.1.2: https://github.com/laozhangge/obsidian-trilium-sync/releases/tag/v0.1.2

---

## 2026-05-28（Bug 修复 — 列表间空行过多 v1）

### 问题

用户日记使用 `* **🌅 早晨：** 内容` 格式，段落间一个空行，同步到 Trilium 后变成 3-4 行空行。

### 根因分析

`prepareTextContent` 的 step 15 中，`___BRBR___` 保护机制阻止了结构性块元素之间多余空行的清理：

1. 原始 `\n\n` → step 12 → `<br><br>`
2. step 15 将 `<br><br>` 保护为 `___BRBR___`
3. 只清理了单个 `<br>`，`___BRBR___` 被跳过
4. 恢复后 `</ul><br><br><ul>` 中的 `<br><br>` 存活
5. Trilium 渲染：列表底部 margin(1行) + `<br><br>`(2行) + 列表顶部 margin(1行) = **4 行**

### 修复

新增 `structuralTags` 白名单（`ul|ol|blockquote|aside|hr|table|pre|h[1-6]`），在恢复 `___BRBR___` 之前，清理结构性块元素之间的占位符。

**精确规则：**
- `</ul>___BRBR___<ul>` → 清理（列表自带 margin）
- `</blockquote>___BRBR___<ul>` → 清理（blockquote 自带 margin）
- `</p>___BRBR___<p>` → **保留**（段落间空行是用户故意留的）

### 验证

```
tsc --noEmit         ✅ 0 错误
esbuild production   ✅ 构建成功
```

**模拟测试 6/6 通过：**
| 场景 | 结果 |
|------|------|
| 列表之间 | ✅ 空行被清理 |
| 段落之间 | ✅ 空行保留 |
| 标题和列表之间 | ✅ 空行被清理 |
| 段落和列表之间 | ✅ 空行保留 |
| blockquote 和列表之间 | ✅ 空行被清理 |
| 用户实际日记格式 | ✅ 列表间无多余空行 |

### Release

v0.1.1: https://github.com/laozhangge/obsidian-trilium-sync/releases/tag/v0.1.1

---

## 2026-05-28（第四轮修复 — 稳定性审计修复）

### 修复总览

基于第四轮综合审计报告，共修复 6 项问题（中优 3 项 + 低优 3 项），聚焦稳定性与防御性编程。

**修改文件：** `main.ts`、`manager.ts`、`settings.ts`、`types.ts`

---

### 🟡 中优先级修复（3 项）

#### 1. 缺少 rename 事件监听（A9）

**问题：** 用户在 Obsidian 中重命名文件时，旧路径的 idMap 记录不会被更新，新路径会创建新记录，导致 Trilium 中出现两个同名笔记。

**修复：** 在 `main.ts` 中添加 `vault.on('rename', ...)` 事件监听器，调用 `syncManager.renameFileMapping(oldPath, newPath)` 更新 idMap 映射。`manager.ts` 中新增 `renameFileMapping` 方法。

**修改文件：** `main.ts`、`manager.ts`

#### 2. CreateAttributeRequest.type 修复未生效（A1）

**问题：** 上轮审计报告写明已改为 `string`，但实际代码仍然是 `'label' | 'relation'`。

**修复：** 将 `types.ts` 中 `CreateAttributeRequest.type` 改为 `string`，并添加注释说明。

**修改文件：** `types.ts`

#### 3. 表格 style 属性可能触发 WAF（A12）

**问题：** 表格对齐生成 `<th style="text-align:center">` 和 `<td style="text-align:center">`，老张之前反馈过 1Panel WAF 拦截带 `style` 属性的 HTML。

**修复：** 移除表格单元格的 `style` 属性，不再生成对齐样式。表格分隔行的对齐信息仍被解析（用于检测表格结构），但不再输出到 HTML。

**修改文件：** `manager.ts`

---

### 🟢 低优先级修复（3 项）

#### 4. handleDeletions 加 etapi 空值守卫（A10）

**修复：** `handleDeletions` 方法开头加 `if (!this.etapi) return { synced, failed, errors }` 守卫，防御性编程。

**修改文件：** `manager.ts`

#### 5. popup.innerHTML = '' → replaceChildren()（A4）

**修复：** `settings.ts` 中两处 `popup.innerHTML = ''` 改为 `popup.replaceChildren()`，保持安全一致性。

**修改文件：** `settings.ts`

#### 6. scheduleSyncAfterModify 回调加 try-catch（A8）

**问题：** `syncSpecificFiles` 如果抛出异常，错误会被吞掉（unhandled promise rejection）。

**修复：** 包裹 try-catch，错误输出到 console.error。

**修改文件：** `main.ts`

---

### 验证结果

```
tsc --noEmit         ✅ 0 错误
esbuild production   ✅ dist/main.js
```

**产物验证：**
| 检查项 | 结果 |
|--------|------|
| 表格无 style 属性 | ✅ 0 处 |
| renameFileMapping 存在 | ✅ 2 处 |
| rename 事件监听存在 | ✅ 1 处 |
| replaceChildren 存在 | ✅ 2 处 |
| etapi 空值守卫 | ✅ 2 处（syncSingleFile + handleDeletions） |
| CreateAttributeRequest.type:string | ✅ 已生效 |

---

## 2026-05-28（第三轮修复 — 综合审计修复）

### 修复总览

基于第三轮综合代码审计报告，共修复 17 项问题（高优 4 项 + 中优 7 项 + 低优 6 项），跳过 2 项（设计如此 / 低 ROI 重构）。

**修改文件：** `main.ts`、`manager.ts`、`etapi.ts`、`settings.ts`、`idmap.ts`、`tree.ts`、`types.ts`、`esbuild.config.mjs`、`package.json`

---

### 🔴 高优先级修复（4 项）

#### 1. loadSettings 浅拷贝 → 深拷贝

**问题：** `loadSettings()` 返回的数组字段（`mappings`）与 `DEFAULT_SETTINGS` 共享引用，修改 settings 会污染默认值。

**修复：** 在 `main.ts` 的 `loadSettings()` 中，对 `mappings`、`metadataTriliumAttributes`、`defaultSyncBlackList` 三个数组字段做深拷贝。

**修改文件：** `main.ts`

#### 2. Token 输入框隐藏

**问题：** ETAPI Token 以明文显示在设置面板中，屏幕共享或截图时有泄露风险。

**修复：** Token 输入框改为 `type="password"`，添加"显示/隐藏"切换按钮。

**修改文件：** `settings.ts`

#### 3. syncSingleFile 加 etapi 空值守卫

**问题：** `syncSingleFile()` 未检查 `this.etapi` 是否为空，极端情况下（初始化失败、并发状态切换）可能抛出未捕获异常。

**修复：** 方法开头加 `if (!this.etapi) return;` 守卫。

**修改文件：** `manager.ts`

#### 4. initPromise 错误传播 + initFailed 标志

**问题：** `initPromise` 的 catch 分支吞掉了错误，调用方无法感知初始化失败。

**修复：** 新增 `initFailed` 标志，`syncNow()` 检查该标志，失败时 Notice 提示用户检查设置。`startAutoSync` 同样检查。

**修改文件：** `manager.ts`

---

### 🟡 中优先级修复（7 项）

#### 5. saveSettings → saveData（同步方法内）

**问题：** `main.ts` 中 `syncNow`、`onunload` 等同步方法内调用 `await this.saveSettings()` 无法生效（saveSettings 是异步的）。

**修复：** 同步方法内改用 `await this.saveData(this.settings)` 直接持久化。

**修改文件：** `main.ts`

#### 6. innerHTML → 安全 DOM API

**问题：** `settings.ts` 中错误信息使用 `innerHTML` 显示，虽然内容来自内部错误对象，但存在 XSS 隐患。

**修复：** 错误信息改用 `textContent` 赋值，日志详情改用 `createDiv` + `textContent`。

**修改文件：** `settings.ts`

#### 7. EtapiClient 只在 url/token 变化时重建

**问题：** `updateSettings()` 每次被调用都重建 EtapiClient 实例，断开已有连接、丢弃队列。

**修复：** 新增 `etapiConfigChanged()` 方法，比较 url 和 token，仅在变化时重建。`getUrl()` 和 `getToken()` 访问器暴露配置。

**修改文件：** `manager.ts`、`etapi.ts`

#### 8. IdMapManager.all() 深拷贝

**问题：** `all()` 返回 `{ ...this.map }` 浅拷贝，调用方修改 entry 内部属性会影响原始数据。

**修复：** 改用 `JSON.parse(JSON.stringify(this.map))` 深拷贝。

**修改文件：** `idmap.ts`

#### 9. 并发同步跳过时 Notice 提示用户

**问题：** `syncNow()` 在 `syncRunning` 为 true 时静默跳过，用户不知道自己的手动操作被忽略了。

**修复：** 跳过时显示 `⚠️ 同步正在运行中，请稍后再试`。

**修改文件：** `manager.ts`

#### 10. 增量刷新路径 undefined 防护

**问题：** `incrementalRefreshTriliumTreeCache()` 递归遍历时 `folderPath` 可能 undefined。

**修复：** 加 `if (!folderPath) return;` 守卫。

**修改文件：** `main.ts`

#### 11. setInterval syncNow 补 catch

**问题：** 定时器回调中 `syncNow()` 返回 Promise 但未 catch，异常会被吞掉。

**修复：** 补充 `.catch(err => console.error(...))` 。

**修改文件：** `manager.ts`

---

### 🟢 低优先级修复（6 项）

#### 12. esbuild 自动创建 dist 目录

**修复：** `esbuild.config.mjs` 构建前用 `fs.mkdirSync('dist', { recursive: true })` 确保目录存在。

#### 13. 插件目录路径常量化

**修复：** `idmap.ts` 和 `tree.ts` 中硬编码的 `.obsidian/plugins/obsidian-trilium-sync` 改为常量 `PLUGIN_DIR`。

#### 14. obsidian 依赖版本锁定

**修复：** `package.json` 中 `obsidian` 版本从 `latest` 改为 `>=0.15.0 <2.0.0`。

#### 15. CreateAttributeRequest.type 扩展

**修复：** `types.ts` 中 `type` 字段从 `'label' | 'relation'` 改为 `string`，兼容未来 Trilium 新增属性类型。

#### 16-17. 删除死代码

**修复：** 删除未使用的 `SyncLogger` 类（`logger.ts` 已在之前轮次删除）和未使用的 `ChangeEvent`/`ChangeType` 类型定义。

---

### 跳过项（2 项）

| 项 | 原因 |
|----|------|
| onunload 500ms 硬编码等待 | 设计如此，Obsidian 无 API 检测磁盘写入完成 |
| settings.ts 弹出菜单去重 | 240 行重构，低 ROI，不影响功能 |

---

### 验证结果

```
tsc --noEmit         ✅ 0 错误
esbuild production   ✅ dist/main.js 46KB
回归测试 22/22       ✅ 全部通过
```

---

## 2026-05-27（第二轮修复）

### 修复总览

本轮共修复 8 个问题，核心改动集中在 `src/sync/manager.ts` 的 `prepareTextContent()` 方法。

**备份目录：** `src_backup_20260527_final/`、`dist_backup_20260527_final/`

---

### 1. blockquote 多段落支持

**问题：** 正则 `(^>\s*.*$)` 遇到空行就断开，多段落引用被拆成两个独立 blockquote。

**修复：** 改为行级解析器（`inBq` 状态机），空行保留在引用块内，多段落引用合并为一个 `<blockquote>`。

---

### 2. Admonition 语法支持（`<aside>` 格式）

**问题：** Obsidian 的 `> [!NOTE]`、`> [!WARNING]` 等 callout 语法被当成普通文本。

**修复：** 检测 `[!TYPE]` 语法，转换为 Trilium 的 admonition 格式：
```html
<aside class="admonition note"><p><strong>自定义标题</strong></p><p>内容</p></aside>
```
- 支持自定义标题：`> [!note] 一言` → 标题 "一言" 作为 `<strong>` 保留
- 支持无标题：`> [!NOTE]` → 只显示内容
- 类型映射：note/tip/important/caution/warning

新增辅助方法：`buildBlockquoteHtml()`、`splitIntoParagraphs()`

---

### 3. 列表 flush 时机修复

**问题：** `- item\n> quote` 结构中，列表后紧跟 blockquote 时列表内容丢失。

**修复：** 列表处理循环新增 `<blockquote>` 和 `<aside>` 作为 flush 触发条件。

---

### 4. 块级元素间多余 `<br>` 清理

**问题：** `<h1>Diary</h1><br><ul>` 中的 `<br>` 导致 Trilium 渲染时多出空行。

**修复：** 步骤15新增清理逻辑，移除紧贴块级元素的单个 `<br>`。用 `___BRBR___` 占位符保护 `<br><br>`（用户故意留的空行），清理完再恢复。

---

### 5. Checkbox 转换（`[ ]` / `[x]`）

**问题：** Obsidian 的 `- [ ]` 和 `- [x]` checkbox 语法被当成普通文本。

**修复：** 在列表项推入 `listLines` 时检测 checkbox 语法并转换：
- `[ ]` → `<input type="checkbox">`
- `[x]` → `<input type="checkbox" checked>`

---

### 6. 空行（空段落）保留

**问题：** 用户在 Obsidian 中故意留的空行，同步到 Trilium 后消失。

**根因：** 拼接时 `if (curr === '') continue;` 跳过了空行；步骤15的 `<br><br>` 压缩规则也吃掉了空行。

**修复：**
- 拼接时不跳过空行，保留它们变成 `\n\n` → `<br><br>`
- 步骤15用 `___BRBR___` 占位符保护 `<br><br>`，清理完再恢复

---

### 7. Admonition 遇空行自动分隔

**问题：** 多个 admonition 用空行分隔时，被合并成一个大 admonition。

**根因：** 空行被 `isEmpty && inBq` 条件吃进了同一个 blockquote。

**修复：** 如果当前 blockquote 是 admonition（以 `[!TYPE]` 开头），遇到空行就结束。普通引用才保留多段落合并。

---

### 验证结果

所有改动均通过模拟测试：
- `tsc -noEmit` ✅ 通过
- `esbuild production` ✅ 通过
- 产物：`dist/main.js` 43KB

| 测试项 | 结果 |
|--------|------|
| blockquote 多段落合并 | ✅ |
| admonition 转 `<aside>` | ✅ |
| 列表后跟 blockquote 不丢失 | ✅ |
| 块级元素间无多余 `<br>` | ✅ |
| checkbox 转换 | ✅ |
| 空行保留 | ✅ |
| 多个 admonition 各自独立 | ✅ |
| 普通引用多段落仍合并 | ✅ |

---

## 2026-05-27（第一轮修复）

### 修复：blockquote 正则不支持 `> [!note]` 无空格格式

**问题：** `>[!note]` 格式的 blockquote callout 无法被正确识别。

**修复：** 改 `\s+` 为 `\s*`。

**修改文件：** `src/sync/manager.ts`

---

### 修复：列表内容在每个标题下重复

**问题：** 一个标题下的列表内容会重复出现在后续所有标题下。

**修复：** 重写列表处理逻辑为按 HTML 行遍历，遇到 `<h1>`~`<h6>` 时 flush 当前列表并重置上下文。

**修改文件：** `src/sync/manager.ts`

---

### 修复：emoji 内容同步后变成一行

**问题：** 列表处理后拼接非列表行时使用了 `join('')`，把所有非列表行内容串在一起无换行。

**修复：** 改为按行遍历拼接，非列表行之间恢复 `\n` 分隔。

**修改文件：** `src/sync/manager.ts`

---

### 修复：双空行问题（`</blockquote>` 后出现两个 `<br>`）

**问题：** 拼接时 `curr === ''` 的空行被加了 `\n`，导致 `\n\n` → `<br><br>`。

**修复：** 拼接时空行直接跳过。

**修改文件：** `src/sync/manager.ts`

---

### 遗留问题（已知）

1. **baseline 覆盖问题**（Bug 4）：手动运行两次同步后，第二次同步的笔记内容变成了第一次的样子。本着能用就行的原则，暂不修复。
2. **特殊字符串替换问题**（Bug 5）：如果笔记正文中恰好包含 `___CODE_BLOCK_N___` 格式的字符串，会被错误替换。触发条件极低，暂不修复。

**编译状态：** ✅ 通过

---

## 2026-05-03（续6）

### 修复：强制刷新逻辑错误

**问题：** 每点击一次"强制刷新"，目录数增加1（重复添加）；新加的目录没有出现在目录树中。

**修复：** `refreshTriliumTreeCache` 始终全量遍历 + 并发优化；`incrementalRefreshTriliumTreeCache` 单独方法供自动刷新用。

**编译状态：** ✅ 通过

---

## 2026-05-03（续5）

### 修复：强制刷新按钮也走增量逻辑

**问题：** 点击"强制刷新 Trilium 目录树"按钮后，按钮变成"刷新中"十来分钟不可点击。

**修复：** `refreshTriliumTreeCache` 区分首次/非首次：首次全量遍历，非首次增量更新。

**编译状态：** ✅ 通过

---

## 2026-05-03（续4）

### Trilium 目录树刷新优化（并发请求 + 增量更新）

**修改文件：** `src/sync/etapi.ts`、`src/sync/tree.ts`

**优化方案：**
1. 并发请求（默认5并发），使用 `Promise.allSettled` 批量处理
2. 增量更新：首次全量，非首次只遍历变化的子树

**编译状态：** ✅ 通过

---

## 2026-05-03（续3）

### V2 优化修复批次（基于审计报告 V2 新1-新8）

**修改文件：** `src/sync/etapi.ts`、`src/sync/tree.ts`、`src/sync/manager.ts`、`src/settings.ts`

| 修复项 | 说明 | 风险 |
|--------|------|------|
| 新1 | getNoteBranches @deprecated 标记清理 | 无 |
| 新2 | walkTree 重复 API 调用：新增 getChildNoteTitles | 无 |
| 新3 | findOrCreateFolder 缓存 root 标题：5分钟 TTL | 无 |
| 新4 | 引用块合并：多行引用合并为一个 `<blockquote>` | 无 |
| 新5 | 列表嵌套丢失：根据缩进层级生成嵌套的 `<ul><li>` | 无 |
| 新6 | `<oli>` 临时标签冲突：改用 `___OLI_${index}___` | 无 |
| 新7 | testConnection 复用实例 | 无 |
| 新8 | 输入校验：URL 格式校验、同步间隔上限 | 无 |

**编译状态：** ✅ 通过

---

## 2026-05-03（续2）

### V2 审计问题修复批次（基于审计报告 V2 M4/M5/L1）

**修改文件：** `src/main.ts`、`src/settings.ts`、`src/sync/etapi.ts`

| 修复项 | 说明 | 风险 |
|--------|------|------|
| M4 | EtapiClient 实例重复创建 | 无 |
| M5 | getFolderTree 超时后后台任务继续 | 无 |
| L1 | saveSettings 未 await | 极低 |

**编译状态：** ✅ 通过

---

## 2026-05-03（续）

### 性能优化修复批次（基于审计报告 S2/S4/M6/L4）

**修改文件：** `src/sync/manager.ts`、`src/sync/idmap.ts`、`src/sync/etapi.ts`

| 修复项 | 说明 | 风险 |
|--------|------|------|
| S4 | shouldUpdate 内容比较 | 无 |
| S2 | idMap 批量保存 | 无 |
| M6-a | 标题转换：`<b>` → `<h1>`~`<h6>` | 无 |
| M6-f | 代码块格式：使用 `<pre>` 标签 | 无 |
| L4 | findChildByTitle 效率优化 | 低 |

**编译状态：** ✅ 通过

---

## 2026-05-03

### 安全修复批次（基于 2026-05-01 审计报告）

**修改文件：** `src/sync/manager.ts`、`src/sync/tree.ts`

| 修复项 | 说明 | 风险 |
|--------|------|------|
| S3 | 去掉重复的清理无效记录逻辑 | 无 |
| M1 | 删除 resolveParentNoteId 死代码 | 无 |
| M2 | 删除 htmlToMarkdown 死代码（73行） | 无 |
| M3 | 删除 SyncLogger（从未调用） | 无 |
| M7 | tree.ts loadCachedFolders 路径修正 | 无 |
| L2 | startAutoSync 开头先调 stopAutoSync() | 无 |

**代码量变化：** manager.ts 646行 → 532行（减少 114 行）

**编译状态：** ✅ 通过

---

## 2026-05-01

### Bug 修复

| Bug | 状态 | 说明 |
|-----|------|------|
| Bug 1（目录嵌套） | ✅ 已修复 | `triliumFullPath` 去掉 `.md` 后缀，`findOrCreateFolder` 只传父目录路径 |
| Bug 2（笔记类型 text/markdown） | ✅ 已修复 | 改为 `text` 类型 |
| Bug 3（onunload 未同步） | ✅ 已修复 | `onunload` 时先调 `syncNow()` |
| Bug 4（baseline 覆盖） | ❌ 不修复 | 用增量结果覆盖了 baseline |
| Bug 5（特殊字符串替换） | ❌ 不修复 | `___CODE_BLOCK_N___` 格式冲突 |

### 格式转换：Obsidian HTML → Trilium HTML

实现 `htmlToTriliumFormat` 函数，处理 `<font>`、`<br>`、`<hr>`、`***`、`---` 等格式转换。

### 改进：关闭 Obsidian 时强制同步

`onunload` 时先调 `syncNow()` 再停止定时器。
