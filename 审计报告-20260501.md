# obsidian-trilium-sync 代码审计报告

**审计日期：** 2026-05-01
**审计范围：** src/ 下全部 7 个 TypeScript 源文件

---

## 一、严重问题（可能导致数据丢失或同步错误）

### S1. onunload 同步可能静默失败
- **文件：** src/main.ts 第 137-143 行
- **问题：** onunload() 调用 await syncNow()，但 Obsidian 插件的 onunload 生命周期不保证 await 能执行完毕。如果 Obsidian 在 syncNow 完成前就销毁了运行环境，最后一批变更会丢失。
- **影响：** 关闭 Obsidian 前的最后一次修改可能不同步。
- **建议：** 改为在同步队列中维护脏标记，下次启动时补偿同步。

### S2. idMap 每次写操作都全量持久化
- **文件：** src/sync/idmap.ts 第 78-81 行（set）、第 84-87 行（remove）
- **问题：** 每 set/remove 一次就调用 save()，即 JSON.stringify + write。全量同步 100 个文件 = 100 次磁盘写入。
- **影响：** 大量文件时性能差，频繁磁盘 I/O，Obsidian 桌面端可能卡顿。
- **建议：** 改为 debounce 或批量写入（syncNow 结束后统一 save 一次）。

### S3. 清理无效记录与 handleDeletions 重复执行
- **文件：** src/sync/manager.ts 第 183-200 行 vs 第 333-361 行
- **问题：** syncObsidianToTrilium() 先做了一轮"清理无效记录"（删 idMap 中文件不存在的），然后 handleDeletions() 又做了一遍完全相同的逻辑。同一个删除操作可能触发两次 ETAPI deleteNote 调用。
- **影响：** 第二次调用必然 404（已被删），虽然 catch 忽略了，但浪费了 API 请求，且日志会有误导性的错误。
- **建议：** 去掉 syncObsidianToTrilium 中的清理逻辑，只保留 handleDeletions。

### S4. shouldUpdate 的内容比较可能误判
- **文件：** src/sync/manager.ts 第 366-379 行
- **问题：** localContent（原始 Markdown）与 triliumContent（经过 prepareTextContent 转换后的内容）直接 trim 比较。两者的格式完全不同（Markdown vs HTML），几乎永远不相等，导致每次都会触发更新。
- **影响：** 所有已同步但未修改的文件，每次全量同步都会被重新 PUT 一遍。
- **建议：** 比较前将本地内容也经过 prepareTextContent 转换，或者只比较 mtime（当前 mtime 逻辑已经是第一道防线，内容比较是 fallback）。

### S5. startTime 逻辑冗余且可能误跳过文件
- **文件：** src/main.ts 第 17-22 行
- **问题：** if/else 两个分支做了完全相同的事（startTime = Date.now()）。更严重的是，每次 Obsidian 启动都会重置 startTime，意味着如果用户关闭 Obsidian 后在外部编辑了 .md 文件，再次打开时这些文件的 ctime < startTime，会被跳过不同步。
- **影响：** 外部编辑器修改的文件可能被永久跳过。
- **建议：** 只在插件首次安装时设置 startTime，后续启动不再重置。

---

## 二、中等问题（功能缺陷或代码质量）

### M1. 死代码：resolveParentNoteId 方法未使用
- **文件：** src/sync/manager.ts 第 419-436 行
- **问题：** 这个方法在 .bak 文件中有调用，但当前版本的 syncObsidianToTrilium 使用的是 etapi.findOrCreateFolder()。resolveParentNoteId 及其依赖的 treeCache 相关初始化（load、loadCachedFolders、ensureLoaded）都是死代码。
- **建议：** 确认是否需要保留。如果不需要 treeCache，可以移除相关代码减少启动时的 API 调用和内存占用。

### M2. 死代码：htmlToMarkdown 方法未使用
- **文件：** src/sync/manager.ts 第 438-510 行
- **问题：** 73 行的 HTML→Markdown 转换方法从未在当前代码中调用。只在 .bak 文件的旧版同步流程中使用过。
- **建议：** 删除或移到备份文件中。

### M3. SyncLogger 从未写入日志
- **文件：** src/sync/manager.ts 第 34 行创建了 SyncLogger 实例，src/sync/logger.ts 完整实现
- **问题：** syncLogger 在构造函数中创建，但整个 manager.ts 中没有任何地方调用 syncLogger.log()。日志系统完全未启用。
- **建议：** 在 create/modify/delete/rename 操作后调用 log()，或者移除 logger 模块减少打包体积。

### M4. EtapiClient 每次刷新目录树都重新创建
- **文件：** src/main.ts 第 114-115 行（refreshTriliumTreeCache），src/settings.ts 第 476-477 行（testConnection）
- **问题：** 每次调用都 new EtapiClient()，没有复用 manager 中的实例。
- **建议：** 复用 SyncManager 中的 etapi 实例。

### M5. getFolderTree 超时后后台任务继续运行
- **文件：** src/sync/etapi.ts 第 290-302 行
- **问题：** Promise.race 超时后，_getFolderTreeImpl 仍在后台运行，持续消耗 CPU 和网络请求。虽然结果写入共享 resultRef 会最终拿到部分数据，但没有取消机制。
- **建议：** 加 AbortController 或 visited 上限控制。

### M6. prepareTextContent 转换规则有多处缺陷
- **文件：** src/sync/manager.ts 第 526-623 行
- **具体问题：**
  - a) 第 545 行：所有级别标题（#~######）都转为 `<b>`，丢失层级信息
  - b) 第 549 行：`**bold**` 正则不支持跨行匹配
  - c) 第 573 行：引用块每行独立处理，多行引用不会合并
  - d) 第 576-581 行：列表不支持嵌套，所有层级都变成平级 `<li>`
  - e) 第 584 行：有序列表用 `<oli>` 临时标签，但如果输入恰好包含字面量 "\<oli\>" 会出错
  - f) 代码块占位符恢复时（第 614-620 行），代码块内的 `\n` 被转为 `<br>`，但原始代码块应该是纯文本保留换行符

### M7. tree.ts loadCachedFolders 路径可能有误
- **文件：** src/sync/tree.ts 第 58 行
- **问题：** 读取 'data.json'（vault 根目录下的 data.json），但 Obsidian 插件的 data.json 实际在 `.obsidian/plugins/trilium-sync/` 下。如果 vault 根目录恰好没有 data.json，catch 会静默失败。
- **建议：** 使用插件专用路径读取，或从 Plugin 实例获取数据。

---

## 三、低风险问题（改进建议）

### L1. getObsidianFolderTree 的 saveSettings 未 await
- **文件：** src/main.ts 第 53 行、第 89 行
- **问题：** 在 forceRefresh 分支和缓存更新分支都调用了 this.saveSettings() 但没有 await。
- **影响：** 数据可能还没写完就被后续操作读取。

### L2. startAutoSync 没有先停止旧定时器
- **文件：** src/sync/manager.ts 第 83-92 行
- **问题：** 如果 updateSettings 被多次调用且 autoSyncEnabled 都为 true，可能创建多个 setInterval，导致同步频率倍增。
- **注：** updateSettings 中有先 stopAutoSync，所以正常流程没问题，但 startAutoSync 本身没有防护。
- **建议：** 在 startAutoSync 开头加 stopAutoSync()。

### L3. settings.ts 中 TextComponent 的 onChange 太频繁
- **文件：** src/settings.ts 多处
- **问题：** 每个输入框的 onChange 都直接调用 saveSettings()，用户每输入一个字符就会保存一次。
- **建议：** 加 debounce 或改为"保存"按钮统一保存。

### L4. findChildByTitle 效率低
- **文件：** src/sync/etapi.ts 第 180-191 行
- **问题：** 逐个遍历子节点，每个都要发一次 GET /notes/{noteId}。如果父节点有 100 个子节点，查找一次要 100 次 API 调用。
- **建议：** 用 searchNotes 的 search 参数做服务端过滤。

### L5. .bak 文件不应提交到仓库
- **问题：** src/ 下有 3 个 .bak 文件和 1 个 src_backup_20260501 目录。
- **建议：** 加入 .gitignore，用 git 管理版本而非手动备份。

---

## 四、安全审计

### 安全1. ETAPI Token 明文存储
- **状态：** ⚠️ 可接受但需注意
- **说明：** Token 存在 Obsidian data.json 中，Obsidian 本身不加密插件数据。这是 Obsidian 插件生态的通用做法。
- **建议：** 在设置页面的 Token 输入框加上 `type="password"` 属性，防止旁人窥屏。

### 安全2. 无输入校验
- **状态：** ⚠️ 低风险
- **说明：** triliumUrl 没有校验格式，用户输入错误的 URL 会在测试连接时报错，不会导致安全问题。
- **建议：** 加基本的 URL 格式校验。

### 安全3. requestUrl 的 throw: false 处理
- **状态：** ✅ 正确
- **说明：** 手动检查 response.status，错误信息足够清晰。

---

## 五、总结评分

| 维度 | 评分 | 说明 |
|------|------|------|
| 功能完整性 | ★★★★☆ | 核心同步功能完整，但有死代码和未启用功能 |
| 代码质量 | ★★★☆☆ | 有重复代码、死代码、转换规则粗糙 |
| 健壮性 | ★★★☆☆ | 错误处理基本到位，但有静默失败风险 |
| 性能 | ★★☆☆☆ | idMap 频繁写入、findChildByTitle 效率低、shouldUpdate 误判导致重复更新 |
| 安全性 | ★★★★☆ | Token 明文存储是 Obsidian 生态通病 |

**总体评价：** 功能可用，有若干需要修复的实际问题（尤其 S2/S4/S5）。

**优先建议修复顺序：** S4 → S2 → S5 → S3 → M7 → M1/M2/M3 清理
