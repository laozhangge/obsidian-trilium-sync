# trilium-sync 开发方案

> Obsidian → Trilium 同步插件
> 最后更新：2026-04-30
>
> 当前状态：代码骨架完成，TypeScript 编译通过，esbuild 打包成功
> 构建产物：main.js (16KB), styles.css (610B)
>
> ✅ 已完成：目录树缓存 + resolveParentNoteId 实现
> ✅ 已完成：CSS 样式（目录标签样式）
> ✅ 已完成：Trilium 目录刷新按钮（真调用）
> ✅ 已完成：fullSync() 全量同步
> ✅ 已完成：Trilium 路径不存在时自动创建中间目录
> ✅ 已完成：设置界面"全量同步"区块

---

## 一、项目概述

- **项目名称**：trilium-sync
- **功能**：把 Obsidian 的日志、笔记等同步到 Trilium
- **开发起点**：从 0 开始，参考 `/root/obsidian-sample-plugin` 和 Trilium 官方 ETAPI
- **学习优先级**：
  1. Obsidian 插件开发示例：`/root/obsidian-sample-plugin`
  2. Trilium 官方仓库：https://github.com/TriliumNext/Trilium
  3. **ETAPI 规范（重中之重）**：
     - https://docs.triliumnotes.org/user-guide/advanced-usage/etapi
     - https://docs.triliumnotes.org/user-guide/advanced-usage/etapi/api-reference

---

## 二、设置界面要求

### 2.1 连接配置
- **Trilium 网址输入框**：支持域名或 IP+端口
- **ETAPI Token 输入框**
- **测试连接按钮**：反馈连接结果（测 URL 可达 + token 有效 + GET /etapi/version）

### 2.2 自动同步
- **自动同步开关**：启用/禁用
- **同步间隔（秒）**：输入框，最低限 30 秒，防止限流
- 手动同步入口：Obsidian 左侧栏图标（自动同步未选中时可用）

### 2.3 同步目录
- 下拉菜单选择（Obsidian SuggestModal 多选）
- **目录树缓存配置**（`data.json`，放插件 dir 下，不放根目录）：
  - `cachedAt` 时间戳，超过 24 小时自动过期，下次打开后台静默刷新
  - "刷新 Obsidian 目录" 按钮
  - "刷新 Trilium 目录" 按钮
- 首次使用时强制刷新目录树
- 同步目录允许多选

### 2.4 保存配置
- **保存配置按钮**：持久化到插件目录

---

## 三、核心同步逻辑

### 3.1 文件变更检测（关键难点）

> Obsidian 没有直接的"文件监控"钩子，需要组合多个 API。

**启动时建立基准快照：**
```
onload() → vault.getFiles() 快照为 "基准文件列表"（此时为空队列）
```

**后续只处理启动后的变更：**
```
vault.on('create')  → 加入 syncQueue
vault.on('modify')  → 加入 syncQueue
vault.on('delete')  → 加入 syncQueue
vault.on('rename')  → 查 id-map → 重命名处理
```

**禁止同步的文件：** `data.json`、`id-map.json`

### 3.2 Trilium Note ID ↔ Obsidian 文件路径映射表

**文件：`id-map.json`（放插件 dir 下）**

结构：
```json
{
  "/path/to/file.md": { "noteId": "xxx", "title": "yyy", "syncedAt": "ISO时间" },
  ...
}
```

**用途：**
- 删文件时知道该调哪个 ETAPI 删除
- 重命名时知道该更新哪个 Note（而非删旧建新）
- 避免重复创建

### 3.3 同步类型处理流程

**新建（create）：**
```
Obsidian 路径 → 查 id-map（无记录）→ ETAPI 创建 Note
→ 写入 id-map → 记录 syncedAt
```

**修改（modify）：**
```
Obsidian 路径 → 查 id-map（有记录）→ ETAPI 更新 Note content
→ 更新 syncedAt
```

**删除（delete）：**
```
Obsidian 路径 → 查 id-map（有记录）→ ETAPI 删除 Note
→ 从 id-map 移除
```

**重命名（rename）（对应 BUG1）：**
```
旧路径 → 查 id-map → ETAPI 更新 note title（不改 content）
→ 更新 id-map.json（路径替换）
禁止 "删旧建新"，否则 Trilium 历史记录和链接全断
```

### 3.4 dateNote 属性规则（每日一志）

**触发条件：** 同步文件所在一级目录为 `01-每日一志`

**写入位置：** Trilium 的"拥有的属性"（type=label）

**写入内容：**
```
属性名：dateNote
属性值：从文件名截取 年+月+日，如 "2026-04-29"
格式：#dateNote=2026-04-29
```

**注意：** 属性是单独接口 `POST /etapi/notes/{noteId}/attributes`，type 设为 `"label"`，不是基本属性接口。

---

## 四、已知 BUG 修复检查清单

### BUG1：重命名时旧文件没清除，又新建同内容新文件
- **根因**：没维护 id-map，重命名时不知道对应哪个 Trilium Note
- **修复**：维护 id-map.json，重命名时 ETAPI 更新 title，不删旧建新

### BUG2：对打开 Obsidian 之前的旧文件也进行同步
- **根因**：没有建立"基准快照"，所有文件都被当作新建
- **修复**：onload() 时先快照基准文件列表，syncQueue 初始化为空，只有后续变更才入队

### BUG3：拥有属性 #dateNote= 写入不成功
- **根因**：用了基本属性接口而非 label 接口
- **修复**：用 `POST /etapi/notes/{noteId}/attributes`，type="label"

---

## 五、附加要求

1. **同步结果通知**：底部状态栏显示"已同步 X 个文件"或"同步失败 Y 个"
2. **同步间隔下限**：最低 30 秒
3. **data.json 和 id-map.json 放插件 dir 下**（`app.plugins.getPlugin('trilium-sync').dir`），方便备份，不随插件升级丢失
4. **禁止同步 data.json 和 id-map.json**，防止循环或错误写入

---

## 六、开发优先级

1. 先学懂 Obsidian 插件开发示例 + Trilium ETAPI
2. 先写文件变更检测 + id-map 模块，写单元测试
3. 再做设置界面
4. 最后接 Trilium ETAPI 真实调用

---

## 七、参考资源

- Obsidian 插件示例：`/root/obsidian-sample-plugin`
- Trilium 官方仓库：https://github.com/TriliumNext/Trilium
- ETAPI 使用指南：https://docs.triliumnotes.org/user-guide/advanced-usage/etapi
- ETAPI API 参考：https://docs.triliumnotes.org/user-guide/advanced-usage/etapi/api-reference
